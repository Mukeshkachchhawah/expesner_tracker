import 'package:flutter/material.dart';

class CategryPage extends StatelessWidget {
  const CategryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Category"),
      ),
    );
  }
}